import matplotlib.pyplot as plt
import math
import torch
import numpy as np
from pprint import pprint
import pandas
import random
import datetime
import termcolor as color

'''def f(x, x2):
    if x == 0 or x2 == 0:
        return 0
    #if (x/x2) > 1:
        #return x*2
    #if (x/x2) <= 1:
    return x**-1 + x/2


x = []
y = []

for i in range(-5000, 5000):
    if i != 0:
        x.append(i)
        y.append(f(i, random.randint(1, 2)))

plt.plot(x, y)
plt.show()'''

print(np.minimum(np.array([1, 2, 3, 4, 5, 6, 7, 8]), 5))
